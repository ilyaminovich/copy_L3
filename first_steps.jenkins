#!groovy
// Check ub1 properties
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage("First step") {
            steps {
                echo 'Hello'
            }
        }
    }
}